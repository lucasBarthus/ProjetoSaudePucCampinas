using Fusion;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using static Unity.Collections.Unicode;

/*
public class UiController : MonoBehaviour
{

    public FusionManager fusionManager;
    public NetworkRunner runner;
    public PlayerRef player;
    public GameObject canvasCharacterSelct;

    public void Start()
    {
        if (runner == null)
        {
            runner = FindObjectOfType<NetworkRunner>(); // Certifique-se de que está buscando o objeto correto
        }

        fusionManager = GameObject.Find("[Fusion-Manager]").GetComponent<FusionManager>();
    }


    public void OnClickButtonCharacterCreation()
    {

       //   fusionManager.OnCharacterSelectButtonClicked();
        Debug.Log("ckicou no botao 1");
    }
    public void OnClickButtonStart()
    {
        Debug.Log("Clicou no botão Start");

        // Chame a função OnStartButton do FusionManager passando os parâmetros corretos
        if (fusionManager != null && runner != null)
        {
            
            canvasCharacterSelct.SetActive(false);
        }
        else
        {
            Debug.LogWarning("FusionManager ou Runner/Player não está configurado corretamente.");
        }
    }
}





*/